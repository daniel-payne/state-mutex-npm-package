import stringToSimpleType from "../../../lib/esm/hooks/helpers/stringToSimpleType"

describe("stringToSimpleType", () => {
  it("should be there", () => {
    expect(stringToSimpleType).toBeDefined()
  })
})
